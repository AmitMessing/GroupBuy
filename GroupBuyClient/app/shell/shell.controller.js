mainApp
    .controller('shellController', ['$scope', '$element', function ($scope, $element) {
        $scope.isOpen = false;
        $scope.categories = ['Fashion', 'Gifts', 'Home', 'Man Fashion'];

}]);